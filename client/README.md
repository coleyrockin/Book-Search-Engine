# Book Search Engine Client

Vite-powered React client for the Book Search Engine.

```bash
npm start
npm run build
npm run preview
```

The dev server runs on `http://localhost:3000` and proxies `/graphql` to `http://localhost:3001`.
